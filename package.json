{
  "name": "nyt-react-app",
  "version": "1.1.0",
  "description": "NYT Search with React",
  "main": "server.js",
  "engines": {
    "node": ">=11.9.0",
    "npm": ">=6.7.0"
  },
  "scripts": {
    "server": "node server.js",
    "client": "node scripts/start-client.js",
    "start": "concurrently \"nodemon server.js\" \"npm run client\"",
    "build": "node scripts/build.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "installDeps": "npm install && cd client && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.10"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "hoek": "^6.1.2",
    "merge-descriptors": "^1.0.1",
    "mongoose": "^5.4.11",
    "url-parse-lax": "^3.0.0"
  }
}
